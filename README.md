This is a react template with full node.js server using express.js.

Webpack 4 is set up and configured with a dev and production version. 

To Run:

git clone 

yarn 

yarn dev or yarn prod for different env's